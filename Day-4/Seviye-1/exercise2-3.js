let myAge = 21;
let yourAge = prompt("Enter your age");
let diff = Math.abs(myAge-yourAge);
if(myAge>yourAge)
{
    console.log(`I'm ${diff} years older than you`);
}
else
{
    console.log(`You are ${diff} years older than me`);
}
// Egzersizde eşit olma durumu belirtilmediği için göz ardı edilmiştir.
// 3. egzersiz çok basit olduğu için çözmüyorum.